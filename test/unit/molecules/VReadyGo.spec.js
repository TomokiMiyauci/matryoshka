import { shallowMount } from '@vue/test-utils'
import VReadyGo from '~/components/molecules/VReadyGo'

describe('VReadyGo', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VReadyGo)
    expect(wrapper)
  })
})
