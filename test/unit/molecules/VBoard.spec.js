import { shallowMount } from '@vue/test-utils'
import VBoard from '~/components/molecules/VBoard'

describe('VBoard', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VBoard)
    expect(wrapper)
  })
})
