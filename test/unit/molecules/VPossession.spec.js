import { shallowMount } from '@vue/test-utils'
import VPossession from '~/components/molecules/VPossession'

describe('VPossession', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VPossession)
    expect(wrapper)
  })
})
