import { shallowMount } from '@vue/test-utils'
import VAvatarWithName from '~/components/molecules/VAvatarWithName'

describe('VAvatarWithName', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VAvatarWithName)
    expect(wrapper)
  })
})
