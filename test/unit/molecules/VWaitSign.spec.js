import { shallowMount } from '@vue/test-utils'
import VWaitSign from '~/components/molecules/VWaitSign'

describe('VWaitSign', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VWaitSign)
    expect(wrapper)
  })
})
