import { shallowMount } from '@vue/test-utils'
import VGameManager from '~/components/molecules/VGameManager'

describe('VGameManager', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VGameManager)
    expect(wrapper)
  })
})
