import { shallowMount } from '@vue/test-utils'
import VPlayroomTag from '~/components/atoms/VPlayroomTag'

describe('VPlayroomTag', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VPlayroomTag)
    expect(wrapper)
  })
})
