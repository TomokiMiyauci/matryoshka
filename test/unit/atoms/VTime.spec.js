import { shallowMount } from '@vue/test-utils'
import VTime from '~/components/atoms/VTime'

describe('VTime', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VTime)
    expect(wrapper)
  })
})
