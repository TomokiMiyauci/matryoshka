import { shallowMount } from '@vue/test-utils'
import VTurn from '~/components/atoms/VTurn'

describe('VTurn', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VTurn)
    expect(wrapper)
  })
})
