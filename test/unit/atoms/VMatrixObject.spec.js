import { shallowMount } from '@vue/test-utils'
import VMatrixObject from '~/components/atoms/VMatrixObject'

describe('VMatrixObject', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VMatrixObject)
    expect(wrapper)
  })
})
