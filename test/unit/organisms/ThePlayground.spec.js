import { shallowMount } from '@vue/test-utils'
import ThePlayground from '~/components/organisms/ThePlayground'

describe('ThePlayground', () => {
  it('should be ', () => {
    const wrapper = shallowMount(ThePlayground)
    expect(wrapper)
  })
})
