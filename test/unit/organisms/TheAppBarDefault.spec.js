import { shallowMount } from '@vue/test-utils'
import TheAppBarDefault from '~/components/organisms/TheAppBarDefault'

describe('TheAppBarDefault', () => {
  it('should be ', () => {
    const wrapper = shallowMount(TheAppBarDefault)
    expect(wrapper)
  })
})
